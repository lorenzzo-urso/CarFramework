# backend/ — Dev A (cérebro). Python 3.11+
# Instalar num venv proprio:  python -m venv .venv && .venv\Scripts\activate && pip install -r requirements.txt

fastapi>=0.110          # API /conversa (P0-2)
uvicorn[standard]>=0.29 # servidor ASGI para rodar a API
rdflib>=7.0             # ontologia em Turtle / grafo do CAR (P0-4)
anthropic>=0.40         # LLM de traducao (P0-5) — Claude Haiku 4.5
pyyaml>=6.0             # ler o manifesto compadre.yaml (P0-1)
python-dotenv>=1.0      # carregar ANTHROPIC_API_KEY do .env (nunca commitar o .env)
